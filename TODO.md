# To-dos

- [ ] Inline chart rendering: let the user ask for a chart and have it render directly in the chat window, instead of text-only replies.
- [ ] Inline browser control: let the user pull up a web browser inside the chat (e.g. "go to YouTube, pull up this video"), watch it embedded, and interrupt with voice commands ("fast forward to 10 minutes") while still talking to the assistant.
- [ ] Attachment button: let the user upload files into the chat — PDFs, images, and audio (for permanent audio uploads, separate from live conversation).
